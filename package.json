{
    "scripts": {
        "sass": "sass --watch sass:css --style=compressed",
        "serve": "browser-sync start --server --files 'css/*.css, *.html'",
        "watch": "nodemon --exec npm-run-all --parallel sass serve"
    },
    "devDependencies": {
        "nodemon": "^3.1.9",
        "npm-run-all": "^4.1.5"
    }
}
